import { Link } from "react-router-dom";

function Login(){

    return (
        <div>
            <Link to={'/'}>Film</Link>
            <div>Login</div>
        </div>
    )

}

export default Login;